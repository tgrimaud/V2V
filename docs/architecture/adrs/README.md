# Architecture Decision Records

This folder contains Architecture Decision Records (ADRs) for Voice Support Bot.

ADRs capture structural decisions that should remain visible beyond code, chats,
diagrams, and planning notes. When a decision changes, create a new ADR and mark
the previous one as superseded instead of rewriting history.

> **Note (built vs target, refreshed 2026-08-05):** an ADR status of `Accepted`
> records an accepted **target decision**, not necessarily that it is implemented.
> As of the Sprint 11 branch (`feat/sprint-11-remote-deployment`):
>
> - **Built:** the two-service voice loop (ADR-0021/0022/0023/0024/0025/0033/0035),
>   the backend conversation engine, RAG and observability
>   (ADR-0001/0006/0007/0014/0026/0027/0028/0030/0031/0034), the pilot latency
>   taxonomy (ADR-0018/0029), **Redis-backed sessions** (ADR-0008, Sprint 11
>   TASK-BE-021), the **backend↔runtime Flow A SSE style** (ADR-0036),
>   **first-sentence streaming** (ADR-0037 — `VOICE_BACKEND_STREAM` default-ON
>   since TASK-WEB-022; the ADR-0029 end-of-turn gate still applies), and the
>   **pilot deployment** stack (ADR-0038/0039).
> - **Target-only — NOT implemented in code yet:** billing/BSS
>   (ADR-0003/0004/0005/0017), **multi-agent routing** (ADR-0015 — no
>   `IntentClassifier`/`AgentProfile` on this branch), Genesys/escalation handoff
>   (ADR-0019/0020), and omnichannel contracts (ADR-0009/0010/0011).
> - **Accepted, scheduled — not built yet:** ADR-0043 (interim WebSocket audio
>   transport for external browser voice, Genesys-ready — Sprint 12).
> - **Proposed (not decided/built):** ADR-0032; **ADR-0049** (Genesys Audio Connector
>   Sprint 13 delivery shape — spike-gated by OQ-006 + TASK-WEB-025).
>
> Implementation status is tracked in `product-backlog/backlog-index.md` and the
> full adversarial review `docs/architecture/reviews/full-adversarial-review-2026-08-05.md`.

## Index

| ADR | Status | Decision |
|---|---|---|
| [ADR-0001](ADR-0001-java-backend-owns-conversation-domain.md) | Accepted | Java backend owns conversation, RAG, guardrails, routing, escalation, and persistence. |
| [ADR-0002](ADR-0002-pipecat-gradium-target-voice-path.md) | Accepted | Pipecat + Gradium is the target V1 voice path; custom bridge remains legacy/fallback. |
| [ADR-0003](ADR-0003-billing-v1-uses-read-only-bss-and-deterministic-comparison.md) | Accepted | Billing V1 uses read-only BSS data and deterministic invoice comparison before LLM wording. |
| [ADR-0004](ADR-0004-bss-integration-through-typed-domain-ports.md) | Accepted | BSS access goes through typed domain ports and contract-compatible adapters, not runtime MCP. |
| [ADR-0005](ADR-0005-invoice-pdf-extraction-before-llm-explanation.md) | Accepted | Invoice PDFs are extracted into deterministic JSON before comparison and explanation. |
| [ADR-0006](ADR-0006-mistral-chat-and-ollama-embeddings.md) | Accepted | Mistral is the default chat LLM; Ollama `nomic-embed-text` remains the embedding model. |
| [ADR-0007](ADR-0007-source-document-knowledge-sync.md) | Accepted | Knowledge ingestion uses a source-agnostic `SourceDocument` pivot and idempotent sync. |
| [ADR-0008](ADR-0008-redis-active-sessions-postgres-durable-events.md) | Accepted | Redis stores active conversation state; PostgreSQL stores durable events and vector data. |
| [ADR-0009](ADR-0009-independent-channel-adapters-shared-java-backend.md) | Accepted | Omnichannel entry points stay independent while sharing the Java conversation backend. |
| [ADR-0010](ADR-0010-industrialization-requires-contracts-slos-and-observability.md) | Accepted | Industrialization requires channel contracts, escalation contract, measurable SLOs, and observability before adding real channels. |
| [ADR-0011](ADR-0011-voice-channels-through-pipecat-text-channels-to-backend.md) | Accepted | Voice channels go through channel proxies to Pipecat; text channels go directly to the Java backend. |
| [ADR-0012](ADR-0012-modular-voice-pipeline-over-realtime-api.md) | Accepted | Voice support uses a modular STT/RAG/LLM/TTS pipeline instead of an all-in-one realtime provider. |
| [ADR-0013](ADR-0013-tokenstream-and-backend-sse-streaming-contract.md) | Accepted | Backend streaming uses the domain `TokenStream` contract and SSE at the API boundary. |
| [ADR-0014](ADR-0014-domain-guardrails-before-and-after-rag.md) | Accepted | Guardrails run in the backend domain pipeline before and after retrieval. |
| [ADR-0015](ADR-0015-keyword-routing-with-session-stickiness.md) | Accepted | Multi-agent routing uses deterministic keyword scoring with session stickiness. |
| [ADR-0016](ADR-0016-legacy-bridge-is-fallback-and-comparison-path.md) | Accepted | The custom bridge remains a legacy fallback and comparison path, not the V1 target. |
| [ADR-0017](ADR-0017-billing-v1-with-general-support-foundation.md) | Accepted | Billing invoice explanation is the V1 value focus on top of the general support assistant foundation. |
| [ADR-0018](ADR-0018-voice-latency-targets-and-slo-measurement.md) | Accepted | Voice latency uses a shared taxonomy: ~700 ms experience target, p95 < 800 ms pilot criterion, production SLO deferred until observability gates are met. |
| [ADR-0019](ADR-0019-escalation-rules-and-handoff-contract.md) | Accepted | Escalation decisions stay in the backend and future contact-center handoff uses a shared payload. |
| [ADR-0020](ADR-0020-genesys-handoff-v1-full-audio-connector-optional.md) | Accepted | Genesys advisor handoff is V1 scope; full Genesys Audio Connector routing remains optional unless the pilot requires it. |
| [ADR-0021](ADR-0021-conversation-backend-answer-contract.md) | Accepted | The voice runtime talks to the conversation backend through a neutral `BackendAnswerPort` answer contract (stub + HTTP adapters), keeping providers replaceable. |
| [ADR-0022](ADR-0022-webrtc-transport-for-streaming-voice-loop.md) | Accepted | The streaming voice loop uses `SmallWebRTCTransport` driving the Pipecat pipeline on a single long-lived async loop (RF-012); FastAPI-free signaling on `SmallWebRTCConnection`. |
| [ADR-0023](ADR-0023-streaming-stt-transport-and-turn-finalization.md) | Accepted | Streaming STT sends audio to Gradium over a WebSocket while the customer speaks, pushing partials and finalizing on the streaming VAD end-of-turn. |
| [ADR-0024](ADR-0024-streaming-tts-incremental-playback.md) | Accepted | Streaming TTS plays the answer on the first synthesized chunk over the Gradium WebSocket; batch TTS stays as fallback. |
| [ADR-0025](ADR-0025-barge-in-native-interruption.md) | Accepted | Barge-in uses Pipecat's native `InterruptionFrame` mechanism driven by the existing streaming VAD (gated by bot-speaking state); Silero deferred as a drop-in verdict upgrade. |
| [ADR-0026](ADR-0026-backend-runtime-and-ai-framework.md) | Accepted | Backend uses Spring Boot + Spring AI for V1 (resolves OQ-007); Quarkus + LangChain4j deferred to an ops/agentic trigger; providers stay behind replaceable ports. |
| [ADR-0027](ADR-0027-backend-modular-decomposition-knowledge-conversation.md) | Accepted | Backend is a Hive-light modular monolith with two context-first bounded contexts (`knowledge`, `conversation`); the sole seam is `KnowledgeRetrievalPort` + an INPROC ACL; orchestration is domain logic, ports are streaming-ready, memory and guardrails sit behind ports/domain services. |
| [ADR-0028](ADR-0028-backend-observability-correlation-and-slice-metrics.md) | Accepted | Backend observability (TASK-BE-009): a `CorrelationIdFilter` + MDC gives runtime→backend correlation-id continuity (body id authoritative on `/converse`), and a `BackendTelemetry` component records the `voice_support.slice` Micrometer timer (slice/channel/provider/outcome, p50/p95/p99) for retrieval/LLM/request slices, exposed via actuator with no collector; instrumentation stays at the infra boundary and a Tracing→OTel bridge is the upgrade path to spans. |
| [ADR-0029](ADR-0029-pilot-latency-criterion-real-backend-and-market-baseline.md) | Accepted | Revises the ADR-0018 pilot latency criterion for a real backend (resolves OQ-005): the stub-era `p95 < 800 ms` becomes mouth-to-ear p95 ≤ 1.5 s / `time_to_first_audio` p95 ≤ 1.2 s, grounded in 2026 market data; STT/TTS are at the Gradium floor, sub-second is a speech-to-speech property, so ADR-0012 (modular cascade) is reaffirmed and "OpenAI" for V1 means the cascade chat provider, not Realtime S2S. |
| [ADR-0030](ADR-0030-csv-knowledge-connector-and-domain-classification.md) | Accepted | Extends the ADR-0007 KB socle with a `CsvArticleConnector` (`csv-article`, `document_id` as source id) that parses `articles.csv` via commons-csv (RFC-4180) and cleans HTML with jsoup before chunking; a new `DomainClassifierPort` assigns a domain at ingestion, default `EmbeddingDomainClassifierAdapter` (Ollama `nomic-embed-text` cosine vs domain anchors, threshold, else `general`), reused by EPIC-011 at query time. Both deps are pinned (not BOM-managed); batch insert is TASK-BE-014. |
| [ADR-0032](ADR-0032-retrieval-quality-strategy-and-vector-store.md) | Proposed | Retrieval quality strategy and vector store (pgvector vs Qdrant): keep pgvector for V1; framing + lever order recorded, decision deferred until BUG-003 is fixed and retrieval quality is re-measured (OQ-008). |
| [ADR-0033](ADR-0033-webrtc-single-live-voice-transport.md) | Superseded by [ADR-0046](ADR-0046-websocket-primary-live-voice-transport.md) | WebRTC is the **single live (customer-facing) voice transport** for the web channel: all real-time turns (streaming STT in, streaming TTS out ≈ 360 ms first audio, barge-in with browser-native AEC) go through the `web_voice` WebRTC path (ADR-0022). A browser-facing WebSocket voice transport is **rejected** — it would duplicate the live path and drop native echo-cancellation/noise-suppression/jitter/Opus with no latency or quality gain (would reintroduce the ADR-0025 barge-in self-interruption). WebSocket stays the **server↔provider** streaming transport (voice-agent ↔ Gradium STT/TTS). The batch HTTP `/turn` path (≈ 14 s time-to-first-audio) is **offline/tests only**, retained as the deterministic QA/latency harness, never a live path. Web-only scope; telephony/Genesys media is out of scope. |
| [ADR-0031](ADR-0031-answer-language-handling.md) | Accepted | Answer language handling (TASK-BE-015): the assistant answers in the customer's question language (FR/EN) via an `AnswerLanguage` value object (detection heuristic + per-call LLM directive + hand-off markers) and a `LanguageDetector` domain service (per-turn, session stickiness, configurable default `en` for the Eir pilot). The decided language is threaded to the LLM adapter (directive appended last), the guardrail wording follows the same detector, `OutputGuardrail` matches every language's hand-off markers, and `voice_support.answer_language` + `[LANGUAGE]` logs make the per-turn choice observable. Closes the ADR-0030 answer-language open question. |
| [ADR-0034](ADR-0034-kb-audience-boundary-and-weak-confidence-clarify.md) | Accepted | KB audience boundary + weak-confidence clarify (BUG-005): a deterministic `AudienceClassifierPort` (`KeywordAudienceClassifierAdapter`) tags each article `customer`/`internal` at ingestion (JSONB `audience` metadata, defaults `customer`); `PgVectorStoreAdapter.search` always excludes `internal` on the customer answer engine (fail-closed, requires a full re-sync). `RetrievalConfidenceGuardrail` gains a `CLARIFY` band between the floor (`0.5`) and a clarify ceiling (`0.62`), and `InputGuardrail` clarifies vague/low-info continuers (`vas-y`…) before retrieval. Billing proof threshold (OQ-002) stays out of scope. |
| [ADR-0035](ADR-0035-conversational-end-of-call-on-customer-farewell.md) | Accepted | Conversational end-of-call on a customer farewell (US-041, TASK-WEB-010): a deterministic, no-LLM `ClosingIntentDetector` matches a config-tunable FR closing-phrase list as a word-boundary token subsequence, rejecting negations (`non, pas au revoir`) and closing words embedded in a longer request. On a standalone closing, a `CallEndFarewellProcessor` (inserted between STT and the answer step) speaks a confirmation ("Souhaitez-vous autre chose ?") and ends the call only on an explicit "done" confirmation **or** a bounded confirmation-scoped silence timer (`VOICE_FAREWELL_CONFIRM_TIMEOUT_S`, not a general silence-timeout — OQ-041-c stays out of scope); any other utterance cancels the farewell and is answered normally. Ending reuses the TASK-WEB-008 `drain()` path and records a `voice.call_end` reason (`customer_farewell` vs `client_stop`/`client_drop`) under the call correlation id. Phrase sets are FR, env-tunable (`VOICE_FAREWELL_*`). |
| [ADR-0037](ADR-0037-first-sentence-backend-streaming-to-tts.md) | Accepted | First-sentence backend answer streaming to TTS (TASK-WEB-015 lever 1): consume the backend **SSE** `converse-stream` on the Flow A per-turn path and hand the **first guardrail-passing, sentence-sized** chunk to the streaming TTS (plain `TextFrame`, barge-in unchanged) instead of waiting for the whole answer (~−700 to −900 ms). **Gated on DEC-002 safety**: a streamed sentence may be spoken only if the backend guarantees the stream is **vetted-only** (grounding + confidence gate passed) or carries an early authoritative confidence signal — otherwise the runtime does not speak partial content. Ships behind a **default-off** flag, enabled only after the DEC-002 SSE contract is confirmed and a warm+cold **live** before/after sample beats the TASK-WEB-014 baseline. Lever 2 (connect-time warm-up, mirror `TtsSessionWarmer`) recorded here, scheduled with the live pass; lever 3 (env-tunable end-of-turn hold) already implemented. |
| [ADR-0038](ADR-0038-pilot-deployment-architecture-eir-ai4cc-tst.md) | Accepted | Pilot deployment on **eir-ai4cc-tst** (Rocky EL9, bare VMs): **Docker images + docker-compose** per tier, **HAProxy/Keepalived** VIPs (voice `.10` TLS edge → bridges; backend `.11` internal → backends), **Redis-backed conversation memory** (activates ADR-0008 for the 2-instance backend), and a **GitHub Actions build/test/image + Ansible/SSH deploy** release with tag-based rollback. Embeddings placement (Ollama CPU vs Mistral embeddings) and provider egress resolved in ADR-0039 (TASK-INFRA-003); OTLP collector out of scope (no collector host). |
| [ADR-0039](ADR-0039-embeddings-placement-and-provider-egress-tst.md) | Accepted | **Embeddings placement + provider egress on tst** (TASK-INFRA-003, addendum to ADR-0006/ADR-0038 §6): embeddings run on **Ollama `nomic-embed-text` (768 dim) as a CPU sidecar co-located per backend VM** (compose service, internal-only, model pulled at deploy) — no `vector_store` recreation, no cloud egress for RAG, no cross-VM hop/SPOF on the retrieval hot path. **Mistral embeddings rejected** for the pilot (1024-dim recreation + full re-sync + per-query cloud egress). Backend/ollama share the 8 GB VM (5 g/3.0 + 2 g/1.0). Only pilot egress: `api.mistral.ai:443` (chat), Gradium `:443` (STT/TTS), registry `:443` (pulls) — allowlist/proxy a platform input. |
| [ADR-0036](ADR-0036-backend-voice-runtime-communication-style.md) | Accepted | Communication style backend ↔ voice runtime: **Flow A** (live turn: transcript → answer → TTS, and intra-turn signals like the filler trigger) stays **synchronous request/response** over HTTP, using the backend's **SSE** stream (TASK-BE-007) for intra-turn streaming/signals on the *same in-flight request*; gRPC/WebSocket are the sanctioned evolution, **not** a broker. A **message broker (RabbitMQ/Kafka) is rejected on Flow A** (adds latency to a latency feature, RPC-over-broker correlation, at-least-once/ordering → filler after the answer or wrong turn) and **reserved for Flow B** (post-call/async/fan-out/omnichannel — targeted ~Sprint 12; RabbitMQ-vs-Kafka deferred to that ADR). The **filler phrase (TASK-WEB-019/US-020)** is broker-free: V1 = runtime-local `AnswerProcessor` timer + one generic phrase; tailored per-intent fillers = an early intra-turn event on the existing SSE stream (the split-arch equivalent of Pipecat `on_function_calls_started`). Port abstraction keeps a future Flow B broker an adapter change, not a rewrite. |
| [ADR-0040](ADR-0040-genesys-audio-connector-v2v-media-plane.md) | Accepted (target) | Genesys **Audio Connector** (the **bidirectional** feature of the AudioHook protocol) is the target V2V media plane, not bare AudioHook (the **Bot Transcription Connector** feature is listen-only). Splits the Genesys integration into three planes — **media** (Audio Connector, Architect Call Audio Connector action forks + pauses the flow), **control/routing** (Architect + Platform API owns transfer/queue on escalation), **context/handoff** (escalation payload on Architect variables / conversation attributes, backend-owned) — and records the Audio Connector constraints (premium, ≤5 integrations, one bidirectional stream/session, IVR channel, 15 min default cap, PCMU/L16 codecs) plus the overlap where Genesys-native barge-in/end-of-turn/playback events supersede the runtime's bespoke detectors (ADR-0025). Refines ADR-0020; NOT IMPLEMENTED (Sprint 13, gated by OQ-006). |
| [ADR-0042](ADR-0042-no-turn-for-pilot-genesys-audio-connector-external-media.md) | Accepted | **No STUN/TURN for the pilot.** Pilot validation proved the full server-side voice journey works VM↔VM and through the HTTPS edge, but external-browser WebRTC **audio** fails: with `VOICE_STUN`/`VOICE_TURN` empty the bridge only advertises its private host ICE candidate, unreachable off-subnet (signalling succeeds, media is silent). Provisioning a public TURN relay (coturn, public IP/DNS, UDP relay range, credentials) is **not justified** because the target external media plane is **Genesys Audio Connector** (ADR-0040) — a Genesys-initiated `wss://` AudioHook stream that needs no TURN. Decision: keep TURN unprovisioned; scope direct WebRTC (ADR-0033) to same-subnet/dev; deliver external audio via Genesys Audio Connector; keep a **WebSocket audio transport** as an interim lever only if a pre-Genesys external demo is needed. Refines ADR-0033/0040. |
| [ADR-0043](ADR-0043-interim-websocket-audio-transport-genesys-ready.md) | Accepted | **Interim WebSocket audio transport** for external-browser voice (Sprint 12): a `wss://` client→server path (JSON control frames + binary PCM16/16 kHz audio, AudioHook-shaped) through the existing HAProxy edge — implements ADR-0042 point 4, keeps **no TURN**. **Refines** (does not supersede) ADR-0033: a browser WebSocket voice transport is rejected as a same-subnet duplicate but **accepted as the external-reach path** where WebRTC media cannot reach. Designed as reusable capital for **Genesys Audio Connector** (ADR-0040, Sprint 13): a transport-agnostic session factory extracted from `WebRtcSignalingService`, a PCM16/16 kHz internal boundary with codec conversion inside each adapter, and a pluggable control-signal seam (energy/amplitude detectors on WS/WebRTC; Genesys protocol events later). Interim only; TCP head-of-line + weaker AEC than WebRTC are accepted trade-offs. |
| [ADR-0044](ADR-0044-pilot-data-tier-resilience-spof-acceptance-and-graceful-redis-degradation.md) | Accepted | **Pilot data-tier resilience.** Formally **accepts the single-instance Postgres + Redis SPOF** for the pilot (backup/restore as the durability net; managed HA stays the `infra-v1.md` production target), and **reduces the Redis blast radius**: with `CONVERSATION_STORE=redis` + `REDIS_HEALTH_ENABLED=true` a Redis outage flips `/actuator/health` DOWN and HAProxy pulls **every** backend node → full outage from a memory-only dependency. Decision: Redis-down must **degrade, not outage** — fall back to in-process memory and stay in rotation, Redis becomes a degraded/observable (not hard-readiness) signal; multi-turn context loss during the outage is the accepted caveat. Postgres stays fail-safe (existing fallback). Implementation: **TASK-BE-030**. |
| [ADR-0041](ADR-0041-postgres-schema-management-with-liquibase.md) | Accepted | Postgres schema is managed by **Liquibase YAML in the backend** (TASK-INFRA-009), superseding the implicit Hibernate `ddl-auto: update` + Spring AI `initialize-schema: true` + ad-hoc superuser SQL. Split by the connect-as-app-user constraint: **app schema** (`vector_store` reproducing Spring AI 1.0.0's exact DDL — `metadata json`, `embedding vector(768)`, `spring_ai_vector_index` HNSW cosine — + `kb_source_state`, guarded `not tableExists`/`MARK_RAN`) runs at **startup** as the app user (`ddl-auto: none`, `initialize-schema: false`); the **privileged bootstrap** (`CREATE EXTENSION vector`/`uuid-ossp` + grants) runs once at deploy Step 4 via a **one-shot `podman run liquibase`** superuser container with its own tracking tables; only `CREATE DATABASE`/`ROLE`/`ALTER … OWNER` stay a psql pre-step (Liquibase can't create the DB/role it connects as, and the app-user secret stays out of every changelog). Local dev creates the extensions via a pgvector init script. |
| [ADR-0045](ADR-0045-llm-provider-model-benchmark-for-backend-first-token.md) | Proposed | **LLM provider/model benchmark for backend first-token (cascade chat, Lever B).** After WEB-035 capped the STT tail and WEB-036 (top-k 8→5) showed LLM first-token is ~87% of `backend_first_token`, the backend first-token (p95 ~1199 ms) is the largest remaining reducible slice of the ADR-0029 gate and is model-inherent. Scopes ADR-0029 **Direction A** only (cascade chat LLM, not Realtime S2S): benchmark **Mistral small/large + a co-located Ollama model + OpenAI gpt-4o-mini** on TTFT + grounding/quality + cost + data residency, keeping the choice behind the replaceable `LlmPort` (ADR-0026). **Decision deferred** to the data from the spike **TASK-BE-033**; ADR moves to Accepted once a provider/model is selected. |
| [ADR-0047](ADR-0047-single-async-http-websocket-server-one-port.md) | Accepted (target — impl. pending TASK-WEB-038) | **Unify the voice runtime onto a single async HTTP+WebSocket server on one port** (aiohttp preferred, spike-gated), running on the existing background asyncio loop and serving the static UI, `/api/voice/*` REST and the live-audio WebSocket upgrade together. **Refines** ADR-0022 (reverses its stdlib/"no FastAPI" constraint now that ADR-0046 made WebSocket primary and the stdlib server is the blocker). Once HTTP+WS share one routed port, **HAProxy `mode http` tunnels the upgrade on the existing `voice_bridges` backend with no LB config change** → the TASK-WEB-037/INFRA-010 `voice_ws` ACL/backend + `firewall_extra_ports:[8091]` become **removable** and the platform-team edge dependency disappears; it also **lifts the one-call-per-bridge cap** (`SingleClientWebsocketServerTransport`) and is the right foundation for the Genesys Audio Connector `wss://` endpoint (ADR-0040). Business logic unchanged (transport/adapter-layer change; ADR-0043 session factory reused); batch `/turn` contract preserved + re-tested; re-validate the ADR-0029 latency gate after. TASK-WEB-037 stays the interim **bridge**; this ADR is the **destination**. |
| [ADR-0049](ADR-0049-genesys-audio-connector-sprint-13-delivery-shape.md) | Proposed (spike-gated) | **Genesys Audio Connector Sprint 13 delivery shape.** Refines ADR-0040 for Sprint 13: the Audio Connector `wss://` media plane is delivered as **one more transport adapter** on the ADR-0047 single async server (reusing the ADR-0043 session factory + PCM16/16 kHz boundary), codec (PCMU/L16 ↔ PCM16) inside the adapter; control/routing stays in Genesys Architect + Platform API; the `EscalationHandoff` (ADR-0019) is transported **by reference** (`handoff_id` + backend fetch, backend-owned) to dodge Architect variable size limits; barge-in/end-of-turn are owned **per path** (Genesys native events on the Genesys path, in-house detectors on WS/WebRTC dev); degraded modes **fail safe to the advisor queue**; the path stays a **measured spike off the ADR-0029 critical path** with per-leg latency under one trace + a minimal concurrency ceiling; the ADR-0001 Genesys boundary holds. **Gated by OQ-006 + the TASK-WEB-025 go/no-go**; the R1–R6 Must-fix items are the spike's outputs and OQ-006 decision items. |
| [ADR-0046](ADR-0046-websocket-primary-live-voice-transport.md) | Accepted | **WebSocket is the primary V1 live voice transport; WebRTC demoted to optional same-subnet/dev.** Supersedes ADR-0033. Rationale: the V1 production ingress is Genesys-mediated and Genesys streams call audio over `wss://` (Audio Connector / AudioHook, ADR-0040), external-browser WebRTC audio fails without TURN — deliberately unprovisioned (ADR-0042) — and the v0.6.0 pilot proved WebRTC media cannot traverse the containerised bridges (only `8090/tcp` published, container-internal ICE candidates, no UDP path). The AudioHook-shaped WebSocket path (ADR-0043) reaches externally over TCP through the HAProxy edge with **no TURN** and is the direct substrate for Genesys Audio Connector. WebRTC is retained (not deleted) as the on-net low-latency dev option (native AEC/jitter/Opus); Pipecat+Gradium (ADR-0002) and the modular cascade (ADR-0012) are unchanged. Puts the `wss` edge routing (**TASK-INFRA-010**) + `ws.js` same-origin fix on the V1 critical path (**TASK-WEB-037**). |
