# Adversarial Review Findings Register

Central log of **non-blocking** findings raised by `adversarial-code-review`
across delivered tickets. Blocking findings are fixed before merge and are not
tracked here; QA defects go through the bug-ticket process instead
(`product-backlog/templates/bug-ticket-template.md`).

Each finding records: source ticket, severity, the accepted residual risk, and
either a follow-up ticket (when the fix is actionable now) or the dependency
that gates it.

## Status legend

- **Open** — accepted residual risk, not yet addressed.
- **Ticketed** — a follow-up ticket exists to fix it.
- **Gated** — cannot be actioned until a dependency is resolved (linked).
- **Closed** — resolved; keep the row for history.

## Findings

| ID | Source | Severity | Finding | Residual risk accepted | Disposition | Status |
|---|---|---|---|---|---|---|
| RF-001 | TASK-STT-003 | Low | Failure sanitization (`sanitization.py`) only redacts tokens containing a path separator; a bare sensitive identifier (e.g. `secret-customer.wav` with no slash) would not be redacted. | Safe today because `FixtureSttProvider` always emits full paths. Risk appears only with a real provider adapter. | **Resolved (TASK-STT-005, 2026-07-10):** `_redact_token` now also redacts bare filenames (`<redacted-file>`) and identifier-like tokens (`<redacted-id>`: UUID, secret prefixes, ≥7-digit runs, mixed ids); words/dates preserved; `error_code` + 160-char cap kept. Dedicated `test_sanitization.py`. | Closed |
| RF-002 | TASK-STT-003 | Low | `stt.audio.accept` span uses `path.exists()` as a channel-ingress analog, not a real ingress measurement. | Acceptable for the scaffold; the STT slice itself is correctly isolated. | **TASK-WEB-001** adds the real `web.voice.ingress` span (received audio bytes) for the web path. Remaining: phone/Genesys ingress and per-slice reporting via **US-036**. | Ticketed |
| RF-003 | TASK-STT-002 | Medium | Transcription quality numbers reflect the deterministic `FixtureSttProvider`, not a real STT engine. | Explicitly disclosed in `docs/qa/stt-transcription-quality.md`; harness re-runs unchanged against a real provider. | **Resolved (TASK-STT-008 + TASK-STT-007, 2026-07-10):** Gradium validated live end to end (`docs/qa/web-voice-qa-report.md`) and a full **live per-category Gradium run** over the 22-fixture set is recorded with normalized WER (`docs/qa/stt-transcription-quality.md`). Residual quality caveat (synthetic proxies → real human recordings) tracked as an open risk, not a scaffold-vs-engine gap. | Closed |
| RF-004 | TASK-STT-002 | Low | Silence/unusable audio maps to outcome `failed` (`invalid_fixture`) rather than a dedicated `UNAVAILABLE` outcome. AC allows either. | Behaviour is correct (no invented transcript); only the outcome label is coarse. | Follow-up ticket **TASK-STT-006** | ✅ Resolved (TASK-STT-006, 2026-07-13) — dedicated `UNAVAILABLE` outcome + `no_speech` code |
| RF-005 | TASK-STT-002 | Low | One fixture per category; p95/p99 and per-category quality are not yet statistically meaningful. | Sprint open question already flags required sample size. | **Resolved (TASK-STT-007, 2026-07-10):** 5 samples per usable category + per-category aggregation; `MIN_SAMPLES_FOR_PERCENTILES=5` flags underpowered categories. Residual (documented open risk): real human recordings still needed for representative `short`/`noisy` and stable p95/p99. | Closed |
| RF-006 | TASK-WEB-001 | Low | The web ingress endpoint (`POST /api/voice/stt`) has no authentication or identity gate; anyone reaching the host can post audio. | Ingress is a local pilot/dev server; the web voice identity model is explicitly deferred, and the backend (not the channel) owns billing-data exposure. | Gated by **OQ-001** (web voice identity) and **TASK-WEB-003** (backend orchestration / identity confidence). | Gated |
| RF-007 | TASK-WEB-001 | Low | `server._read_body` reads exactly `Content-Length` bytes and does not support chunked `Transfer-Encoding`. | Browser `fetch` with an `ArrayBuffer` body always sets `Content-Length`, which is the only client for this slice. | Follow-up ticket **TASK-STT-010** (streaming STT) adds the chunked/streaming ingress transport that closes this. | Ticketed |
| RF-008 | TASK-STT-002 / TASK-STT-007 | Medium | `word_error_rate` compares raw whitespace-split tokens with no normalization. Against a real engine this over-penalizes: `Bonjour` vs `Bonjour.` scores WER 1.0; ASCII references (`telephone`) vs accented output (`téléphone`) count as errors. The `ready` quality gate is therefore not usable against Gradium. | Surfaced by the first live Gradium per-category run (2026-07-10, `docs/qa/stt-transcription-quality.md`): 3/4 usable categories "failed" almost entirely on punctuation/case/accent artifacts, not real STT errors. | **Resolved (TASK-STT-011, 2026-07-10):** `normalize_transcript` folds case/punctuation/accents before WER. Live re-run: `short` WER 1.00→0.00, `long` 0.083, `accented` 0.182 pass; only `noisy` (0.40) fails on a genuine error (→ TASK-STT-007). Gate now trustworthy; threshold kept at 0.8. | Closed |
| RF-009 | TASK-STT-005 | Low | The sanitization identifier heuristics over-redact non-sensitive technical tokens: `pcm_16000` → `<redacted-id>`, `audio/pcm` → `<redacted-path>`, `line 12345678` → `<redacted-id>`. Diagnostic value is reduced. | Safety-first trade-off: over-redaction never leaks data, it only makes some error reasons less specific. Real sensitive tokens are still caught. | **Resolved (2026-07-10, `task/sprint-2-review-remediation`):** `_redact_token` now checks a `_SAFE_TOKENS` allowlist (audio formats `pcm_16000`/`ulaw_8000`, content-types `audio/pcm`/`audio/basic`, etc.) *before* the path/id heuristics, so those stay readable. Residual: bare numeric runs (e.g. `line 12345678`) are still redacted by design (indistinguishable from ids). Tests in `test_sanitization.py`. | Closed |
| RF-010 | TASK-STT-007 | Low | Fixture references have two sources of truth kept in sync by hand: `generate_fixtures.py` `SPOKEN_SPECS[].text` and `manifest.json` `reference`. A silent divergence would break the fixture-provider WER (which assumes sidecar == reference). | Currently consistent (generator writes the sidecar from the same text); risk is future edits touching only one side. | **Resolved (2026-07-10, `task/sprint-2-review-remediation`):** `test_manifest.py::test_manifest_reference_matches_sidecar_transcript` asserts every usable manifest `reference` equals its generated `.txt` sidecar, failing CI on any drift. | Closed |
| RF-011 | TASK-STT-007 | Low | `all_categories_significant` includes the `silence` category (n=2, no WER), so it is effectively always `false`; and `MIN_SAMPLES_FOR_PERCENTILES=5` marks a category "significant" though 5 samples give a statistically weak p95 (nearest-rank p95 ≈ max). | Documented as a reporting floor, not a guarantee (`stt-transcription-quality.md` "Statistical significance"). | **Resolved (2026-07-10, `task/sprint-2-review-remediation`):** `all_categories_significant` and `underpowered_categories()` now consider only categories with `usable_count > 0`, so `silence` no longer drags the aggregate to `false` or shows as underpowered. Residual (accepted): the `MIN_SAMPLES_FOR_PERCENTILES=5` floor remains statistically weak for p95/p99 until real recordings exist — still documented as a reporting floor. | Closed |
| RF-012 | TASK-WEB-005 | Low | `PipecatTurnProcessor` calls `asyncio.run(...)` per request (transcribe/synthesize/turn), spinning up and tearing down a fresh event loop + `PipelineTask`/`PipelineRunner` each turn. Correct under the threaded stdlib `http.server` (each request thread has no running loop), but `asyncio.run` raises `RuntimeError` if ever driven from an already-running loop. | Acceptable for the batch parity migration on the current synchronous HTTP transport; it is not a latency sprint and the overhead is small (~2 ms p50 per the A/B harness). | Gated by **Sprint 6 (SPRINT-6-STREAMING)**: the async WebRTC transport must drive the pipeline by awaiting it (single long-lived loop), not via `asyncio.run` per turn. The `~80% reusable` design note in `sprint-4-pipecat-batch.md` already anticipates this. | Gated |
| RF-013 | TASK-WEB-005 | Low | On a non-success turn, the STT/TTS `error_reason` echoes the raw provider exception text verbatim into the `POST /api/voice/turn` (and `/stt`, `/tts`) 502 JSON body (e.g. `str(exc)`). Sanitization redacts paths/ids/secrets (RF-001/RF-009) but not generic internal wording; a real provider exception could surface internal context to the client. Pre-existing STT/TTS-slice behaviour that the new combined endpoint inherits. | Dangerous tokens (paths, UUIDs, secret prefixes, filenames, long digit runs) ARE redacted; only generic free text passes, and fixtures/stubs emit benign text. Same trade-off already accepted for `/api/voice/stt`. | Follow-up ticket **TASK-WEB-006** (`tasks/web-voice-tasks.md`): return a generic `error_code` + correlation id to external clients and keep the raw reason server-side only (mirrors the Java `GlobalExceptionHandler` `ERR_UPSTREAM` lesson). Regression covered by `test_voice_runtime.py::test_turn_endpoint_fails_closed_with_json_when_stt_fails`. | Ticketed |
| RF-014 | TASK-WEB-005 | Low | `POST /api/voice/turn` adds a third unauthenticated ingress route with no identity gate — the same exposure already logged as RF-006 for `POST /api/voice/stt`, now widened to the full-pipeline endpoint. | Local pilot/dev server; the web voice identity model is explicitly deferred and the backend (not the channel) owns billing-data exposure. | Gated by the same dependencies as RF-006: **OQ-001** (web voice identity) and **TASK-WEB-003** (backend orchestration / identity confidence). | Gated |
| RF-015 | TASK-WEB-003-A | Low | `AnswerResult.confidence` is an unvalidated `float \| None` with no 0..1 range enforcement at the contract level; a provider could emit an out-of-range value that a later degraded-threshold check misreads. | Contract-only ticket with no runtime decision yet; the degraded/confidence threshold logic lives in the runner (TASK-WEB-003-E) and the real threshold is gated by OQ-002. | Gated by **TASK-WEB-003-E** (runner clamps/validates confidence) and **OQ-002** (proof/confidence threshold). | Gated |
| RF-016 | TASK-WEB-003-A | Low | `AnswerResult.error_reason` is a free-text field; when populated by the future HTTP adapter it could carry raw provider text (same class of risk as RF-013 for STT/TTS). | No adapter exists yet in this ticket (stub/HTTP come later); the contract simply reserves the field. | To fix in **TASK-WEB-003-C / TASK-WEB-003-F**: adapters must sanitize via `voice_common.sanitize_error` before setting `error_reason` (mirrors the RF-013 / `ERR_UPSTREAM` lesson). | Gated |
| RF-017 | TASK-WEB-003-B | Low | The stub's DEC-002 guarantee (no fabricated amount) rests on `STUB_ANSWER_TEXT` being digit/currency-free; this is enforced only by `test_stub_backend_adapter.py`, not by the code path. A future edit of the constant that adds a digit would ship a fabricated-amount risk if the test were also relaxed. | The invariant is a compile-time constant covered by an explicit no-digit / no-`€` test, so drift fails CI; the real answer engine (HTTP adapter) replaces this text entirely. | Accepted residual risk. Optional hardening: assert the digit/currency-free invariant at import time. Superseded once the HTTP backend (**TASK-WEB-003-C**) becomes the default answer source. | Open |
| RF-018 | TASK-WEB-003-B | Low | The stub emits `outcome=SUCCESS` with `confidence=None`; a later degraded-threshold check (TASK-WEB-003-F) that treats "missing confidence" as "low confidence" would wrongly degrade every stub turn. | Correct by design (the offline stub cannot compute a confidence and must not fabricate one); no runtime consumer of `confidence` exists in this ticket. | **Resolved in TASK-WEB-003-F**: `_apply_confidence_policy` degrades only when `confidence is not None and confidence < threshold`, so `confidence=None` is treated as "not provided" (skipped) — the stub's SUCCESS turns are never degraded. Covered by `test_high_confidence_answer_is_kept` + the stub parity test. | Resolved |
| RF-019 | TASK-WEB-003-D | Medium | The web page (`web_voice/static/app.js`) was repointed from the two-step `/stt`+`/tts` echo to the single `/turn` answering loop, but the frontend has no automated test — only the server/runtime path is covered by unit + behave tests. A regression in the JS (header decode, playback) would not be caught by CI. | The runtime loop (the ticket's core) is fully covered on both runtimes; the browser layer is thin (fetch `/turn`, decode `X-Voice-*` headers, play WAV) and reuses the existing capture/playback code. | Gated by **TASK-WEB-003-G**: manual Chrome DevTools MCP re-validation of the live answering loop (needs a mic + Gradium key) is an explicit G step. A JS unit/E2E harness remains out of scope for V1 pilot. | Gated |
| RF-020 | TASK-WEB-003-D | Low | When the backend yields no speakable answer (`UNAVAILABLE` / empty text / `EmptyTranscriptError`), `run_turn` returns no `tts_response` and `/turn` replies `502 {"error":"no_audio"}` — a generic failure, not a safe spoken fallback. | Cannot happen with the stub (always `SUCCESS`); the code fails closed (never invents audio) and keeps the correlation id. | **Resolved in TASK-WEB-003-F**: `answer_with_telemetry` degrades any backend fault / low-confidence / empty answer to the safe `DEGRADED_FALLBACK_TEXT` (a DEGRADED result), which is spoken back as a WAV (200) with `X-Answer-Outcome: degraded` + sanitized `degraded_reason`. Only a truly empty transcript (`EmptyTranscriptError`) stays silent by design. | Resolved |
| RF-021 | TASK-WEB-003-E | Low | `answer_with_telemetry` emits `backend.first_token` and `backend.request` with the **same** duration because the stub/HTTP-batch backend returns the whole answer at once — `backend.first_token` is not a real first-token measurement yet. | Correct for a non-streaming backend (first token == full answer); the US-036 slice picks `first_token` first and never sums both spans, so there is no double counting. Documented in `answer.py`. | Gated by a **streaming backend** (TASK-WEB-003-C HTTP streaming / TASK-WEB-004): stamp `backend.first_token` at the first chunk and `backend.request` at completion so the two diverge meaningfully. | Gated |

## Process

Non-blocking findings from every adversarial review **must** be appended here
before a branch is declared merge-ready (see `docs/operations/development-workflow.md`).
Actionable findings get a follow-up ticket in `product-backlog/tasks/`; gated
findings link the blocking dependency.
